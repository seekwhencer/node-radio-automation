language: node_js
node_js:
  - "12"

install:
  - cd app && npm install
  - npm run dist
  - cd ../frontend && npm install
  - npm run build

after_script:
  - ls -l
  - cd ../app && ls -l

deploy:
  provider: releases
  api_key: "32b359488a69b4299da8e892f216dc3448d90d06"
  file: "../app/station"
  skip_cleanup: true
  on:
    tags: true

