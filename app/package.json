{
  "name": "node-radio-automation",
  "version": "0.0.1",
  "description": "audio streaming playlist automation",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "export NODE_ENV=default && node index.js",
    "prod": "export NODE_DEBUG=true && NODE_ENV=production && node index.js",
    "custom": "export NODE_DEBUG=true && NODE_ENV=custom && node index.js"
  },
  "author": "Matthias Kallenbach",
  "license": "GPL-3.0",
  "dependencies": {
    "dateformat": "^3.0.3",
    "fs-extra": "^7.0.1"
  }
}
